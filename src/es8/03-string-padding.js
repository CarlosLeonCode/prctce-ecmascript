// El método padStart completa un string con otro 
// string en el inicio hasta tener un total de caracteres especificado.
const txt = ''
console.log(txt.padStart(100, '😮'))

// El método `padEnd` completa un `string` con otro `string` **en el final** hasta tener un total de caracteres especificado.
console.log('Wooo'.padEnd(100, 'o'))
